#This calculates the deviation between two points.
#It returns the absolute value of the difference between the two points
absolute_difference <- function (num1,num2) 
{
  d=num1-num2
  d= abs(d)
  return(d)
}